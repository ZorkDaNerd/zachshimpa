{
  "dependencies": {
    "three": "^0.152.2"
  },
  "devDependencies": {
    "vite": "^4.3.8"
  },
  "scripts": {
     "test": "echo \"Error: no test specified\" && exit 1",
     "deploy": "gh-pages -d dist"
  }
}
